let number = rand(100);
let tip;
println("I have a number im my mind between ""0 and 100"", let see if you can find out")

for {
    println("Your tip?")
    tip = strToNum(input())

    // Check logic
    if (tip < number) {
        println("No, it's bigger")
    } else {
        println("No, it's smaller")
    }
}(tip != number)

println("Heureka, you got it")
