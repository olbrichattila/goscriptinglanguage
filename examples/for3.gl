let text = "hello";
for (let i = 0;i<len(text);i = i + 1) {
    println(numToStr(i) + " " + substr(text, i, i+1) )
}
// x = 10;
let ln = len(text) - 1;
for (let z = ln;z >= 0;z = z = z -1) {
//for (let z = len(text);z >= 0;z = z = z -1) { // Todo check why errors out
    println(numToStr(z) + " " + substr(text, z, z+1) )
}